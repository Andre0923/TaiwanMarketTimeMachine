# 非意圖性錯誤檢查架構設計

> **撰寫日期**：2026-01-22  
> **最後更新**：2026-01-23  
> **用途**：定義 SpecKit/FlowKit 非意圖性錯誤檢查的分層架構  
> **狀態**：設計文件（v2.0）

---

## 1. 問題背景

### 1.1 關鍵概念澄清

> **核心認知**：Feature Spec 是「更新來源」，其與 System Spec 的差異是**意圖內的變更**，不是錯誤。

| 概念 | 正確理解 | 錯誤理解 |
|------|----------|----------|
| **Feature ≠ System** | 這是意圖，Feature 的目的就是帶來變更 | ❌ 這是「不一致」需要修正 |
| **一致性檢查** | 檢查「非意圖性錯誤」（疏忽、手誤） | ❌ 檢查「Feature 與 System 的差異」 |

### 1.2 現行挑戰

在 SpecKit 開發流程中，**非意圖性錯誤**可能在多個階段發生：

| 問題類型 | 說明 | 發現時機（現行） | 修正成本 |
|----------|------|------------------|----------|
| 重複實作 | Plan 規劃的功能，System 已有類似實作 | 晚期 / Code Review | 高 |
| 遺漏複用 | 應該複用的共享服務未被引用 | 晚期 / Code Review | 高 |
| 非意圖變動 | 實作涉及 Spec 未聲明的變更區域 | Unify Flow | 高 |
| 編號/引用錯誤 | 引用的 ID 不存在或格式錯誤 | Unify Flow | 中 |
| 命名不一致 | 未遵循專案命名慣例 | Code Review | 中 |
| Spec 品質不足 | Spec 結構不完整、內容模糊 | Unify Flow | 中 |

**核心問題**：缺乏系統性的「非意圖性錯誤」檢查機制

---

## 2. 解決方案：兩階段檢查架構

### 2.1 架構總覽

```
┌─────────────────────────────────────────────────────────────────────┐
│  Stage 1: Plan 階段後（/flowkit.consistency-check）🆕               │
│  ├─ 觸發時機：/speckit.plan 完成後、/speckit.tasks 執行前            │
│  ├─ 檢查重點：規劃階段的非意圖性錯誤                                  │
│  │   - 是否重複寫功能或規格                                          │
│  │   - 該引用或複用的功能未引用而重寫                                 │
│  │   - 改寫了非意圖內的變動                                          │
│  │   - 有更佳的整體規劃方向建議                                       │
│  │   - 編號引用錯誤、命名不一致等                                     │
│  └─ 效果：Shift-Left，在規劃階段攔截疏忽                              │
├─────────────────────────────────────────────────────────────────────┤
│  Stage 2: Unify Flow 前（/flowkit.pre-unify-check）                 │
│  ├─ 觸發時機：實作完成後、Unify Flow 執行前                           │
│  ├─ 檢查重點：Spec 品質 + 實作對齊 + 最終攔截                         │
│  │   - Spec 結構完整性、格式正確性                                   │
│  │   - 引用正確性（ID、路徑、命名）                                   │
│  │   - Spec 與實作結果對齊                                           │
│  │   - 最後一道防線（捕捉遺漏問題）                                   │
│  └─ 效果：守護 System Spec 品質                                      │
├─────────────────────────────────────────────────────────────────────┤
│  Unify Flow Phase 5：合併操作驗證（已精簡）                          │
│  ├─ 目的：驗證「合併操作本身」是否正確                                │
│  └─ 效果：僅檢查合併操作的正確性，非全系統掃描                         │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 核心原則：意圖優先

**檢查的是「非意圖性錯誤」，不是「差異」**

| 類型 | 說明 | 是否檢查 |
|------|------|----------|
| **意圖內的變更** | Feature Spec 聲明要修改 X | ❌ 不檢查（這是目標） |
| **非意圖性錯誤** | 疏忽、手誤、重複、引用錯誤 | ✅ 檢查（這是問題） |

### 2.3 兩階段分工

| 階段 | 指令 | 時機 | 檢查重點 | 目標 |
|------|------|------|----------|------|
| **Stage 1** | `/flowkit.consistency-check` | Plan 後 | 重複、複用、範圍、細節 | Shift-Left |
| **Stage 2** | `/flowkit.pre-unify-check` | Unify 前 | Spec 品質、實作對齊 | 品質守門 |

---

## 3. Stage 1 詳細設計：Plan 階段後一致性檢查

### 3.1 設計目標

1. **Shift-Left**：在規劃階段就攔截非意圖性錯誤
2. **最小 Token**：遵循漸進式揭露協議
3. **條件適用**：全新功能自動跳過部分檢查

### 3.2 觸發時機

```
/speckit.specify → /speckit.plan
                        │
                        ▼
            ┌────────────────────────────┐
            │ /flowkit.consistency-check │ ← Stage 1
            └────────────────────────────┘
                        │
                PASS? ──No──→ 修正 Plan → 重新檢查
                        │
                       Yes
                        │
                        ▼
                /speckit.tasks → /speckit.implement
```

### 3.3 檢查項目清單

| 類別 | ID | 檢查項目 | 說明 | 嚴重性 |
|------|-----|----------|------|--------|
| **A. 重複實作** | A1 | 功能重複 | Plan 規劃的功能與現有功能高度相似 | HIGH |
| | A2 | 模組重複 | Plan 新增的模組名稱與現有模組相似 | MEDIUM |
| **B. 複用遺漏** | B1 | 共享服務未使用 | project-context.md 列出的共享服務未被引用 | HIGH |
| | B2 | 工具函數重寫 | Plan 設計了已存在的工具函數 | MEDIUM |
| **C. 非意圖變動** | C1 | 範圍溢出 | Plan 涉及 Spec 未聲明的變更區域 | HIGH |
| | C2 | 隱性依賴 | Plan 的修改會影響未列出的模組 | MEDIUM |
| **D. 細節誤用** | D1 | ID 引用錯誤 | 引用的 UI ID / Entity ID 不存在 | HIGH |
| | D2 | 命名不一致 | 與專案命名慣例不符 | MEDIUM |
| **E. 整合建議** | E1 | 更好的模組位置 | 非阻擋性建議 | SUGGESTION |

### 3.4 全新功能的處理

| 檢查類別 | 全新功能時的處理 |
|----------|------------------|
| A. 重複實作 | 部分適用（檢查可複用的基礎設施） |
| B. 複用遺漏 | B1 仍適用，B2 視情況 N/A |
| C. 非意圖變動 | N/A（無既有範圍可對比） |
| D. 細節誤用 | 仍適用 |
| E. 整合建議 | 仍適用 |

---

## 4. Stage 2 詳細設計：Unify Flow 前品質檢查

### 4.1 設計目標

1. **品質守門**：確保要合併的 Spec 達到品質標準
2. **實作對齊**：確認 Spec 與實作結果一致
3. **最後防線**：捕捉 consistency-check 後新增的問題

### 4.2 觸發時機

```
/speckit.implement → 測試 & Debug
                          │
                          ▼
              ┌────────────────────────┐
              │ /flowkit.pre-unify-check │ ← Stage 2
              └────────────────────────┘
                          │
                  PASS? ──No──→ 修正 Spec/實作 → 重新檢查
                          │
                         Yes
                          │
                          ▼
                   /flowkit.unify-flow
```

### 4.3 檢查項目清單

| 類別 | 檢查項目 | 說明 | 嚴重性 |
|------|----------|------|--------|
| **Spec 品質** | 結構完整性 | Metadata、Overview、AC 存在 | REQUIRED |
| | 格式正確性 | Markdown 語法、ID 格式 | MEDIUM |
| | 內容品質 | AC 可測試、無模糊用語 | MEDIUM |
| **引用正確性** | ID 引用存在 | UI/Entity/API ID 存在 | HIGH |
| | 命名一致性 | 術語、大小寫正確 | MEDIUM |
| **實作對齊** | AC 有對應實作 | 每個 AC 在 Plan 中有對應 | HIGH |
| | 實作產物存在 | 宣稱的檔案確實存在 | HIGH |
| | 變更範圍一致 | 實際修改與 Spec 聲明一致 | HIGH |
| **最終攔截** | consistency-check 問題追蹤 | 已報告的問題已處理 | HIGH |

### 4.4 全新功能的處理

| 檢查類別 | 全新功能時的處理 |
|----------|------------------|
| Spec 品質 | 完整適用 |
| 引用正確性 | 新定義的 ID 標記為「新增項目」 |
| 實作對齊 | T1-T3（變更追蹤）標記 N/A |
| 最終攔截 | 完整適用 |

---

## 5. Unify Flow Phase 5：合併操作驗證（已精簡）

### 5.1 Phase 5 職責

```markdown
### Phase 5：合併操作驗證

**目的**：驗證本次合併操作的正確性（非全系統一致性）

**前提**：已通過 `/flowkit.pre-unify-check`（Stage 2）

**檢查項目**：
| 檢查對象 | 驗證內容 |
|----------|----------|
| Feature 內容整合 | Feature Spec 是否完整整合至 System Spec |
| 合併區段完整性 | 無重複、無遺漏、無結構破壞 |
| 保留區段完整性 | 未涉及區段確認未被修改 |
| Design 同步 | 本次涉及的 Design 檔案已更新 |
| 無殘留 | 無 Feature 名稱、無實作細節洩漏 |
```

### 5.2 Phase 5 不再包含

| 項目 | 說明 | 移至何處 |
|------|------|----------|
| 全系統一致性掃描 | 過重且時機太晚 | Stage 1 + Stage 2 |
| `/speckit.analyze` 調用 | 那是 Feature 層工具 | 移除 |
| 跨 Feature 衝突檢查 | 應在合併前處理 | Stage 1 |

---

## 6. 實作路線圖

### 6.1 Phase 1：已完成 ✅

- [x] 精簡 Unify Flow Phase 5（移除全系統檢查）
- [x] 移除 `/speckit.analyze` 引用
- [x] 設計兩階段檢查架構

### 6.2 Phase 2：已完成 ✅

- [x] 建立 `flowkit.consistency-check.agent.md`（Stage 1）
- [x] 重新設計 `flowkit.pre-unify-check.agent.md`（Stage 2）
- [x] 更新本架構設計文件

### 6.3 Phase 3：可選強化

- [ ] 強化 `/speckit.plan` 對 project-context.md 的讀取
- [ ] 在 Plan 階段加入術語檢查
- [ ] 記錄潛在衝突風險

---

## 7. 預期效益

| 效益 | 說明 | 量化指標 |
|------|------|----------|
| **Shift-Left** | 問題在 Plan 階段就發現 | 修正成本降低 70%+ |
| **品質守門** | Spec 品質在 Unify 前確保 | 合併後回退率降低 |
| **職責清晰** | 每階段有明確邊界 | 維護複雜度降低 |
| **條件適用** | 全新功能自動簡化檢查 | 無效檢查減少 50%+ |

---

## 8. 附錄：流程圖

### 8.1 完整開發流程（含兩階段檢查）

```
┌─────────────────────────────────────────────────────────────────────┐
│                      SpecKit 開發流程                                │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│  /speckit.specify  ─────→  /speckit.plan                            │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│  /flowkit.consistency-check  ◄────── Stage 1：規劃階段非意圖性錯誤   │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 檢查：重複實作 │ 複用遺漏 │ 非意圖變動 │ 細節誤用 │ 整合建議   │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  輸出：分析報告（確定問題 / 待確認 / N/A）                            │
└─────────────────────────────────────────────────────────────────────┘
                                │
                        PASS? ──No──→ 修正 Plan
                                │
                               Yes
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│  /speckit.tasks  ─────→  /speckit.implement  ─────→  測試 & Debug   │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│  /flowkit.pre-unify-check  ◄────── Stage 2：Spec 品質 + 最終攔截    │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 檢查：Spec 品質 │ 引用正確性 │ Spec-實作對齊 │ 最終攔截        │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  輸出：Unify 準備狀態（READY / NOT_READY）                           │
└─────────────────────────────────────────────────────────────────────┘
                                │
                        PASS? ──No──→ 修正問題
                                │
                               Yes
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│  /flowkit.unify-flow                                                │
│                                                                      │
│  Phase 0-4：合併、更新 Design、封存                                   │
│  Phase 5：合併操作驗證（精簡版）                                      │
│  Phase 6：產生摘要                                                   │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
                           PR & Merge
```

---

> **維護提醒**：本文件應與以下檔案保持同步更新：
> - `flowkit.consistency-check.agent.md`（Stage 1）
> - `flowkit.pre-unify-check.agent.md`（Stage 2）
> - `flowkit.unify-flow.prompt.md`（Phase 5）
